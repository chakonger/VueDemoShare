/**
 * Created by lvshicheng on 2016/11/22.
 *
 * 获取指定的状态
 */
export const devices = ({device}) => {
  return device.devices
}
export const powerList = ({power}) => {
  return power.powerList
}
export const deviceIds = ({power}) => {
  return power.deviceIds
}
